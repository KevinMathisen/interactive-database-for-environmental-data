{
  "name": "server",
  "version": "0.0.1",
  "description": "Express server which serves a react application over https with error handling.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  }
}
